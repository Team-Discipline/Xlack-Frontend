일단 필요한 클래스
Lotto
LottoCard
LottoSim

### **Lotto**
```Java
// ex.lotto
class Lotto {
	public Lotto() {
//이건 random 을 써서 int[] lottoNumber 배열에 저장한다. 그리고 그걸 lotto 번호로 만든다.
	}

	public Lotto(int[] lottoNumber) {
// 사용자로부터 배열을 받아서 그걸 로또 번호로 준다.
	}

	void show() {
//for 문을 돌면서 System.out.print("") 로 출력
	}

	boolean verity(int[] lottoNumber) {
		boolean is_valid = false;
		if (lottoNumber[i] == lottoNumber[j]) {
			is_valid = false;
		} else {
			return true;
		}
	}
	int[] getNumbers(){
	int[] valid_num;
	//유효한 로또 번호면 배열로 변환
	//아니면 null
	return valid_num;
	}
}
```

### **LottoCard**
```java
class Lottocard{
	LottoCard[] mLotto = new LottoCard;
	int[] user_input = {};
	mEffNum = random //으로 값을 준다.(1~5사이)
	// 이걸로 몇개는 자동, 몇개는 수동으로 설정해준다.
	for(int i=0; i<mEffNum; i++){
		Lotto();
	}
	for(int i = 0; i<5-mEffNum; i++){
		Lotto(user_input);	
	}
}
```


### **LottoSim**
```java
class LottoSim{
	public static void main(String args[]){
		int[] user_input1 = {1,2,3,4,5,6};
		int[] user_input2 = {1,2,3,4,5,6};
		int[] user_input3 = {1,2,3,4,5,6};
		Lottocard(user_input1);
		Lottocard(user_input2);
		Lottocard(user_input3);
	}
}
```